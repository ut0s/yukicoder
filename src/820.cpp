/**
  @file 820.cpp
  @title  No.820 Power of Two - yukicoder
  @url https://yukicoder.me/problems/no/820
**/

#include <bits/stdc++.h>
using namespace std;

typedef long long LL;
#define ALL(obj) (obj).begin(), (obj).end()
#define REP(i, N) for (int i = 0; i < (N); ++i)

int main() {
  int N, K;
  cin >> N >> K;

  ;

  cout << (int)(pow(2, N) / pow(2, K)) << endl;
  return 0;
}
